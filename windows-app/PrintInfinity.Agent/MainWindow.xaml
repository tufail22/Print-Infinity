<Window
    x:Class="PrintInfinity.Agent.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PrintInfinity.Agent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Print Infinity Agent — Storekeeper Dashboard">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header / Status Bar -->
        <Border Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="20,16" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <TextBlock Text="Print Infinity Agent" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <Border Background="#E0E7FF" CornerRadius="12" Padding="8,4" VerticalAlignment="Center">
                        <TextBlock Text="Station #1 — Online" Foreground="#4338CA" FontSize="12" FontWeight="Medium" />
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                    <Button Content="Printer Settings" />
                    <Button Content="Refresh Queue" Style="{ThemeResource AccentButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Queue Placeholder -->
        <Grid Grid.Row="1" Margin="24">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                <FontIcon Glyph="&#xE749;" FontSize="48" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                <TextBlock Text="Listening for Print Jobs via Supabase Realtime..." FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" />
                <TextBlock Text="Customer uploads from /web will appear here for storekeeper approval." Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Footer / Privacy Commitment -->
        <Border Grid.Row="2" Padding="16,12" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0">
            <TextBlock Text="🔒 Zero-Disk Persistence: All documents stream directly into printer memory and are purged immediately." FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center" />
        </Border>
    </Grid>
</Window>
